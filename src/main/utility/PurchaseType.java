// Package: main.utility
package main.utility;

// === === === Enum: PurchaseType === === ===

/**
 * Enum to specify the type of purchase (Online or In-Store)
 */
public enum PurchaseType {
    IN_STORE,
    ONLINE
}
